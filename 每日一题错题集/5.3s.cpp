#include<stdio.h>
#include<stdlib.h>
int mainlhihikujn()
{
	printf("%s , %5.3s\n", "computer", "computer");
	int a = 1;
	a++;
	int temp = ++a;
	printf("temp=%d,a=%d", temp,a);
    system("pause");
    return 0;
}
/* 下面叙述不正确的是（D）
A 派生类一般都用公有派生
B 对基类成员的访问必须是无二义性的
C 赋值兼容规则也适用于多重继承的组合
D 父类的公有成员在派生类中仍然是公有的 
*p[2]=*(*(p+0)+2);
float在程序中默认提升为double八字节
0123在C语言中是八进制%o输出八进制，
0x123是16进制，
先构造老子再构造儿子


printf("%%%%\n");
输出%%



765
float ‭0010 1111 1101‬
ieee754
*/